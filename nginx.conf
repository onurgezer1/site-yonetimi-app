server {
    listen 80;
    listen [::]:80;
    server_name smartyonetim.com www.smartyonetim.com;
    root /var/www/html/smartyonetim/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    index index.php;

    charset utf-8;

    # Handle static files
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Handle PHP files
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
    }

    # Security - Deny access to sensitive files
    location ~ /\.(?!well-known).* {
        deny all;
    }

    location ~ /\.ht {
        deny all;
    }

    location ~ /storage/ {
        deny all;
    }

    location ~ /bootstrap/ {
        deny all;
    }

    location ~ /app/ {
        deny all;
    }

    location ~ /config/ {
        deny all;
    }

    location ~ /database/ {
        deny all;
    }

    location ~ /routes/ {
        deny all;
    }

    location ~ /tests/ {
        deny all;
    }

    location ~ /vendor/ {
        deny all;
    }

    location ~ /\.env {
        deny all;
    }

    location ~ /composer\.(json|lock) {
        deny all;
    }

    location ~ /package(-lock)?\.json {
        deny all;
    }

    location ~ /artisan {
        deny all;
    }

    # Cache static assets
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options "nosniff";
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    error_log  /var/log/nginx/smartyonetim_error.log;
    access_log /var/log/nginx/smartyonetim_access.log;
}

# SSL configuration (uncomment and configure when SSL is ready)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name smartyonetim.com www.smartyonetim.com;
#     root /var/www/html/smartyonetim/public;
# 
#     ssl_certificate /path/to/ssl/cert.pem;
#     ssl_certificate_key /path/to/ssl/private.key;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:MozTLS:10m;
#     ssl_session_tickets off;
# 
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
# 
#     add_header Strict-Transport-Security "max-age=63072000" always;
# 
#     # ... rest of the configuration same as above ...
# }